import {makeStyles} from '@material-ui/core';

export const standardStyles = makeStyles(() => ({
    droppableContainers: {
        // '& > *': {
        //     marginRight: 5,
        //     marginLeft: 5,
        // }
    }
}));